enum ActiveStatus {
  ACTIVE
  BANNED
}

enum Role {
  TENANT
  LANDLORD
  ADMIN
}

enum PropertyStatus {
  AVAILABLE
  RENTED
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum ReviewStatus {
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum RentalStatus {
  ACTIVE
  CANCELED
  EXPIRED
}