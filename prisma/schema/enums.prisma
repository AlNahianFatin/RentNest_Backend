enum ActiveStatus {
  ACTIVE
  BANNED
}

enum Role {
  TENANT
  LANDLORD
  ADMIN
}

enum PropertyStatus {
  AVAILABLE
  PENDING
  SOLD
}

enum RequestStatus {
  ACCEPTED
  PENDING
  REJECTED
}

enum ReviewStatus {
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}
